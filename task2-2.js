let a = +prompt("Input first link number");
let b = +prompt("Input second link number");
function sum(first, second) {
  return first + second;
}
console.log(sum(a, b));
alert(`${a} + ${b} = ${sum(a, b)}`);
